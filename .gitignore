# The control key's private half. Never commit it.
keys/
# Generated per-device facts, known_hosts, screenshots taken during setup.
state/
*.png
*.xml
report.txt

# device/CLAUDE.md is a deliverable — the provisioner copies it to the phone as
# the on-device agent's instructions. Negated here because a global gitignore
# commonly excludes CLAUDE.md.
!device/CLAUDE.md
articles/
